#lang plai

(require "grammars.rkt")

;; Algoritmo de sustitución textual.
;; subst: WAE symbol WAE -> WAE
(define (subst expr sub-id val)
    #| Aquí va su código. |#)

;; Interpretación de expresiones WAE.
;; interp: WAE -> number
(define (interp expr)
    #| Aquí va su código. |#)
